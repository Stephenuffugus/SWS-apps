# Plan for 2026-08-20, written at the close of the big night

Last night in one line: Cross Off got its user-tested fix, skywolfstudio.com
went live with the whole suite on it, the hub got the premium redesign plus
install buttons, the brand went singular (Sky Wolf Studio, com.skywolfstudio
packages), Stephen got his D-U-N-S and submitted the Google Play organization
application with docs and website verified, stephen@skywolfstudio.com exists,
Hush shipped a stereo depth round with an audible heartbeat and its tagline,
the home screen icon is Stephen's art, and every em and en dash in visible
copy was rewritten as human punctuation.

## Verdicts to collect (people, not code)

1. **The mom's morning Cross Off test.** She opens the app on a new day and
   yesterday should already be in the flip-back pile. Treat any complaint as
   feedback on the NEW auto-flip build, not the old bar.
2. **Jessie on Sub Plans.** She builds her real sub binder, then shares the
   link/QR to Stephen's phone playing the substitute. Also ask her opinion on
   first-listing order. Sub Plans store kit is staged at
   skywolfstudio.com/brand/sub-plans/ and waits on her verdict.
3. **Stephen listens to the new Hush** (live at lucidwinds.com/hush):
   Width slider 0 vs 70 on headphones, womb heartbeat on the phone speaker,
   the Heartbeat slider in Movement. Penny's bedtime is the real QA.
4. **Google Play org verification status** (play console). Nothing to feed
   it; just check. When it goes green, packaging (below) unblocks.

## Build queue, in order

1. **Fix the shared syntax error** in bill-splitter, bracket-maker and
   packing-list: identical "Unexpected identifier 's'" pageerror in all
   three, pre-existing, found by design/guards.mjs on 2026-08-19. Identical
   message across apps means one shared snippet with a broken apostrophe.
   Something on those pages is genuinely not running.
2. **Play packaging toolchain** (design/findings/PLAY-LAUNCH-DECISION.md
   items 5-9): JDK 17 exactly (box has JDK 25, this is the classic failure),
   `npm i -g @bubblewrap/cli@1.25.0` pinned, first AAB on an internal
   testing track, then the fingerprint dance: Play App Signing SHA-256 into
   apps/.well-known/assetlinks.json (24 placeholders), redeploy, statement
   API check. ORIGIN and PKG_PREFIX already point at skywolfstudio.com /
   com.skywolfstudio.
3. **Stephen's two Firebase console clicks** (still open from domain night):
   Hosting → add www.skywolfstudio.com (DNS already placed), and
   Authentication → Settings → Authorized domains → add skywolfstudio.com
   so Google sign-in works on the four shared apps from the new origin.
4. **Canonical tags** pointing old web.app pages at skywolfstudio.com
   (promised; never hard-redirect, testers' data lives on the old origin).
5. **Hush next steps by his ear's verdict** (hush/DEPTH-PLAN.md): texture
   stereo pass, brown sub-body; move Hush into this repo at
   /hush before any bubblewrap init. Full tagline ("Pick your sound. Lock
   your screen. Go to sleep.") is reserved for the Play listing.
6. **Lucid-winds repo hygiene:** only hush/ was swept for dashes and the
   plural "Sky Wolf Studios" may live elsewhere in that repo; sweep the rest
   next time it is open.

## Parked, deliberately

- Apple App Store ($99/yr): cut until something earns.
- Pi Network pilot: register one app + shared tip Cloud Function; after Play.
- Hostinger email renews ~2027-08: move the address to a free provider then.
- Revenue apps (silent auction kit, transcript, donor letters): the
  "subscription for a once-a-year document" prey from the 2026-08-16 hunt.
