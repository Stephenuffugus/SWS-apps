node_modules/
# Astra Vault's built web export bundles its fonts under assets/node_modules;
# that path is served content, not dependencies, so it rides along.
!apps/astravault/assets/node_modules/
*-debug.log
ui-debug.log
.firebase/
*.zip
design/out/

# Agent scratch: trackfit builds land here mid-workflow. The real
# integration will be a deliberate commit, not a swept-up one.
apps/trackfit/

# Jessie's real plan book — colleague names, her school, personal notes.
# Read for reference, never committed. This repo is public.
*.xlsx
